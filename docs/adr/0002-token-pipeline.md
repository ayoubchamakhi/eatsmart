# ADR 0002: token pipeline

Status: accepted

Use structured design tokens as the only styling source of truth. Generate CSS custom properties for web and typed token maps for native from primitives through semantic and component tokens. Components consume semantic/component tokens; pages do not hard-code visual primitives.
